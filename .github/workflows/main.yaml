name: Send my html to digital ocean droplet
on: push
jobs:
  build:
    name: Hello world action
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - run: whoami
    - run: pwd
    - run: sudo docker ps